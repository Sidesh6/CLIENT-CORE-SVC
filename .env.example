# ==============================================================================
# CLIENT CORE SVC — Configuration
# ==============================================================================

APP_ENV=development
APP_DEBUG=true
APP_PORT=8003
APP_HOST=0.0.0.0

# Database Configuration (SQLite default / PostgreSQL production)
DATABASE_URL=sqlite:///./data/core.db
